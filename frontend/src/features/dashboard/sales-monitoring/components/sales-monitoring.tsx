import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export function SalesMonitoring() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Monitoramento de Vendas</CardTitle>
        <CardDescription>Acompanhamento de vendas em tempo real</CardDescription>
      </CardHeader>
      <CardContent>
        <p className="text-sm text-muted-foreground">Conteúdo será implementado na próxima sprint.</p>
      </CardContent>
    </Card>
  );
}
