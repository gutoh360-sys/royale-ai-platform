import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";

export function MarketplaceOverview() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Marketplaces</CardTitle>
        <CardDescription>Desempenho nos canais de venda</CardDescription>
      </CardHeader>
      <CardContent>
        <p className="text-sm text-muted-foreground">Conteúdo será implementado na próxima sprint.</p>
      </CardContent>
    </Card>
  );
}
